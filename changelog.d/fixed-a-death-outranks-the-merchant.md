- **Drowning as the wave clock ran out no longer leaves a run nothing can finish.** A death and a
  cleared wave both ask for a screen after the same short beat, and going under takes longer than
  falling — so the merchant could get there first. The summary then quietly stood down: the run was
  never ended, `run.json` outlived the player, and the title screen offered to Continue into a
  corpse while the next wave started against a body. A death now outranks everything between waves.
  A merchant that has not opened is refused, one that is already up is taken down, and the run ends
  the moment the death is seen. Held by `tools/verify_run_flow.tscn`.
